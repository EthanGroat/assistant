import os
from conversator import Conversator
from globals import *



if __name__ == '__main__':
    main_process = Conversator()
 # uses USER, AI_NAME, CHAT_MODEL, MAX_GPT_PROMPT_CHARS, SAMPLING_METHOD, LENGTH_VARIANCE, PROMPT_REPEAT_CHANCE, GPT_RESCUE_CHANCE, CUSTOM_CHAT_SETTINGS, ROBOT_TEXT_COLOR, USER_TEXT_COLOR; but I declared these as globals. Refactor later?
